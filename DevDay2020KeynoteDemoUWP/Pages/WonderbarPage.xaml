<winui:Page x:Class="DevDay2020KeynoteDemoUWP.WonderbarPage"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:model="using:DevDay2020KeynoteDemoUWP.Model"
            xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
            xmlns:winui="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            mc:Ignorable="d">

    <winui:Grid>
        <winui:FlipView x:Name="MainGridView"
                        Background="White"
                        ItemsSource="{x:Bind _places}">
            <winui:FlipView.ItemTemplate>
                <winui:DataTemplate x:DataType="model:Place">
                    <winui:Grid>
                        <winui:Image x:Name="PlaceImage"
                                     Source="{x:Bind ImageUri}"
                                     Stretch="UniformToFill" />

                        <winui:Border Margin="40"
                                      HorizontalAlignment="Left"
                                      VerticalAlignment="Bottom">
                            <toolkit:DropShadowPanel HorizontalAlignment="Center"
                                                     VerticalAlignment="Center"
                                                     BlurRadius="24"
                                                     IsMasked="True"
                                                     ShadowOpacity="0.5"
                                                     Color="Black">
                                <winui:TextBlock FontFamily="{StaticResource MontserratBold}"
                                                 FontSize="18"
                                                 Foreground="White"
                                                 Text="{x:Bind CountryName}" />
                            </toolkit:DropShadowPanel>
                        </winui:Border>
                    </winui:Grid>
                </winui:DataTemplate>
            </winui:FlipView.ItemTemplate>
        </winui:FlipView>
    </winui:Grid>
</winui:Page>
