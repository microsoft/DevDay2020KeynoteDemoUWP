<Page x:Class="DevDay2020KeynoteDemoUWP.Pages.DestinationsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:model="using:DevDay2020KeynoteDemoUWP.Model"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      mc:Ignorable="d">

    <Page.Resources>
        <CollectionViewSource x:Name="PlacesSource" IsSourceGrouped="True" />
    </Page.Resources>

    <Grid>
        <GridView ItemsSource="{x:Bind PlacesSource.View}">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="model:Place">
                    <Image Width="256"
                           Height="172"
                           Source="{x:Bind ImageUri}" />
                </DataTemplate>
            </GridView.ItemTemplate>

            <GridView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate x:DataType="model:GroupInfoList">
                            <TextBlock Text="{x:Bind Key}" />
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </GridView.GroupStyle>
        </GridView>
    </Grid>
</Page>
